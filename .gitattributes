# Shell scripts and the Dockerfile must use LF: a CRLF shebang fails with
# "bad interpreter", which would silently break the InfluxDB init script
# on a Windows checkout.
*.sh text eol=lf
Dockerfile text eol=lf
